# ngrok 設定ファイル（LINE BOT開発用）
# 詳細: https://ngrok.com/docs/configuration/

version: 2

authtoken: ${NGROK_AUTHTOKEN}
region: ${NGROK_REGION}

# サブドメイン指定（有料プランのみ）
# subdomain: ${NGROK_SUBDOMAIN}

# トンネル設定
# FastAPIアプリ（ポート8000）を外部公開
# Webインターフェース: http://localhost:4040

# tunnels:
#   linebot:
#     proto: http
#     addr: 8000
#     inspect: true
#     schemes:
#       - https
#     # サブドメイン利用時
#     # subdomain: ${NGROK_SUBDOMAIN}

# ログ・監視
log: stdout
log_level: info 